// Crear un componente CommentContent que renderice un componente p y que pueda contener texto dentro suyo
import React from "react";

const CommentContent = ({ texto }) => {
  return <p>{texto}</p>;
};
export default CommentContent;
